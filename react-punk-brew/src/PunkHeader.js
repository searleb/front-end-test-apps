import React from 'react'
import './PunkHeader.css'

class PunkHeader extends React.Component {
  render() {
    return (
      <nav>
        <h2>Brew Dog Library</h2>
      </nav>
    )
  }
}

export default PunkHeader
